{
  "name": "mercury-cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js  --bundle --outfile=build.cjs --format=cjs --platform=node",
    "package": "pkg build.cjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@postlight/parser": "^2.2.3"
  },
  "devDependencies": {
    "esbuild": "^0.19.3",
    "pkg": "^5.8.1"
  }
}
